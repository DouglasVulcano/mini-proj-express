MINI PROJETO EXPRESS

Criar API fake para auxilio no desenvolvimento front-end

Buildar o container
```
docker-compose up -d --build
```

Instalar o express dentro do container
```
docker-compose run app sh

npm install
```